{
	"name": "not-so-good-stats",
	"version": "1.0.0",
	"description": "An app to track stats from the Faceit platform",
	"main": "index.js",
	"scripts": {
		"start": "node server",
		"server": "nodemon server",
		"client": "npm run serve --prefix client",
		"dev": "concurrently \"npm run server\" \"npm run client\""
	},
	"author": "Maciek Papciak",
	"license": "ISC",
	"dependencies": {
		"concurrently": "^5.3.0",
		"cors": "^2.8.5",
		"dotenv": "^8.2.0",
		"express": "^4.17.1",
		"helmet": "^3.23.3",
		"morgan": "^1.10.0",
		"node-fetch": "^2.6.1"
	},
	"devDependencies": {
		"nodemon": "^2.0.4"
	}
}
